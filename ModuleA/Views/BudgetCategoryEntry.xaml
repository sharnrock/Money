<UserControl x:Class="ModuleA.Views.BudgetCategoryEntry"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBox Text="{Binding CategoryName, UpdateSourceTrigger=PropertyChanged}" Grid.Column="0" Margin="5" />
        <TextBox Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" Margin="5" />
    </Grid>
</UserControl>
